package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant Dark Palette Colors
val DarkBg = Color(0xFF0F1115)           // Elegant dark midnight background
val DarkSurface = Color(0xFF161B22)      // Deep slate/navy-tinted surface container
val GoldAmber = Color(0xFFFBE090)        // Spiritual golden light accent (amber-200/300)
val PureGold = Color(0xFFE0A96D)         // Traditional warm gold accent
val SoftEmerald = Color(0xFF10B981)      // Peaceful green/emerald accent
val SlateText = Color(0xFFE2E8F0)        // Soft high-contrast text label
val SlateSubText = Color(0xFF94A3B8)     // Subtitles text level
val AccentLightAmber = Color(0xFFFEF3C7)  // Warm focus card container text

// Light mode fallback (still cohesive, but light-spirited gold & slate)
val LightBg = Color(0xFFF8FAFC)
val LightSurface = Color(0xFFFFFFFF)
val DarkText = Color(0xFF0F172A)
